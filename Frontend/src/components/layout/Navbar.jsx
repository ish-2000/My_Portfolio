// Navbar — now rendered inside Hero.jsx as a floating pill nav.
// This component returns null to avoid duplicate navbars.
// WebLayout still imports it but renders nothing here.
export default function Navbar() {
  return null
}
