// useScrollReveal.js — creates a GSAP ScrollTrigger reveal for a given ref
// Accepts: ref, options
// Returns: nothing (side effect only)
export function useScrollReveal(ref, options = {}) {
  // Placeholder — full implementation in next prompt
}
