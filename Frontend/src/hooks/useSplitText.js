// useSplitText.js — splits a string into word <span> elements
// Accepts: a string
// Returns: array of word span elements (used by Philosophy section)
export function useSplitText(text = '') {
  // Placeholder — full implementation in next prompt
  return []
}
